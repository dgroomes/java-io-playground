package dgroomes;

/**
 * This enum models the different options that the program takes.
 */
public enum WriteOption {

    LARGE,
    TRUNCATE,
    APPEND,
    SUBSTITUTIONS,
    COMPRESS_GZIP,
    COMPRESS_ZLIB,
    COMPRESS_NONE
}
